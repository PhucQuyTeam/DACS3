<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="8dp"
        android:background="@android:color/white"
        android:padding="16dp">

    <!-- Header Đánh giá -->
    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <TextView
                android:id="@+id/tvReviewTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Đánh giá sản phẩm"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#212121"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"/>

        <!-- Tổng sao & Số đánh giá (VD: ⭐ 4.9 (120)) -->
        <TextView
                android:id="@+id/tvAvgRating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⭐ 4.9/5"
                android:textColor="#FF5722"
                android:textStyle="bold"
                android:layout_marginTop="4dp"
                app:layout_constraintTop_toBottomOf="@id/tvReviewTitle"
                app:layout_constraintStart_toStartOf="parent"/>

        <TextView
                android:id="@+id/tvTotalReviews"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="(120 đánh giá)"
                android:textColor="#757575"
                android:textSize="13sp"
                android:layout_marginStart="8dp"
                app:layout_constraintBottom_toBottomOf="@id/tvAvgRating"
                app:layout_constraintTop_toTopOf="@id/tvAvgRating"
                app:layout_constraintStart_toEndOf="@id/tvAvgRating"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Danh sách review xem trước (Chỉ nạp 2-3 item vào đây) -->
    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvPreviewReviews"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:nestedScrollingEnabled="false"/> <!-- Bắt buộc để không kẹt cuộn -->

    <!-- Nút xem tất cả -->
    <com.google.android.material.button.MaterialButton
            android:id="@+id/btnViewAllReviews"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Xem tất cả đánh giá >"
            app:strokeColor="#FF5722"
            android:textColor="#FF5722"/>

</LinearLayout>